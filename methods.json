{
  "sdk.getDrive(url).mirror(out, options)": {
    "properties": {
      "url": {
        "type": "string"
      },
      "out": {
        "type": "string"
      },
      "options": {
        "type": "object",
        "properties": {}
      }
    },
    "required": ["url", "out"]
  },
  "sdk.getDrive(url).version": {
    "properties": {
      "url": {
        "type": "string"
      }
    },
    "required": ["url"]
  }
}
